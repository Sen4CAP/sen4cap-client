For the generation of the documentation the notebooks from `../notebooks` will 
be copied into this folder. This happens on each `mkdocs serve` or `mkdocs build`
with the help of the mkdocs hook `docs/hooks/notebooks_json_output.py`.
