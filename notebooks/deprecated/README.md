# Archived notebooks

These notebooks preserve examples from older client APIs. They are excluded
from the documentation build and are not compatible with the current API.

Use the active notebooks in the parent `notebooks/` directory:
`client-api.ipynb`, `client-cli.ipynb`, and `client-gui.ipynb`.
